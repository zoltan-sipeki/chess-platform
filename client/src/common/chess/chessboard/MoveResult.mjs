export class MoveResult {
    constructor(color, move, scoreboard, nextTurn) {
        this.color = color;
        this.move = move;
        this.scoreboard = scoreboard;
        this.nextTurn = nextTurn;
    }
}